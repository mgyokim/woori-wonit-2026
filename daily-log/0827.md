# Day 2 — 2026.08.27 (목)

> Git 되돌리기 / AI 코딩 도구 · 프롬프트 엔지니어링 · AI 디버깅

- 실습 레포지토리 : [mgyokim/woori-wonit-ai-coding](https://github.com/mgyokim/woori-wonit-ai-coding)
  (원본 [YeonjiKim0316/woori-wonit-ai-coding](https://github.com/YeonjiKim0316/woori-wonit-ai-coding) fork)

---

## 오늘의 핵심 3가지

**1. 한 번이라도 커밋한 건 사실상 안 사라진다. 커밋 안 한 변경은 진짜 사라진다.**
`git reflog` 는 Git의 블랙박스다 — `git log` 는 지금 남은 기록만, `reflog` 는 날린 것까지 보여준다.
반대로 `git restore` 로 지운 **미커밋 변경은 복구 수단이 없다.** "자주 커밋하라"는 말의 진짜 이유.
(뒤집으면, **지우고 싶은 파일도 안 사라진다**는 뜻이다.)

**2. AI는 맞는 코드가 아니라 "그럴듯한" 코드를 만든다.**
다음에 올 말을 **확률로** 고르는 것이지 맞는지 틀리는지를 판단하는 게 아니다.
자신 있게 틀리고, 미안해하지도 않는다. 환각·지식 단절·맥락 이탈이 전부 여기서 파생된다.

**3. "안 돼요"는 프롬프트가 아니다.**
`[재현 조건] / [기대 동작] / [실제 동작]` 3종 세트로 묻는다.
그리고 **"먼저 설명하고 그다음 코드"** — 고친 코드만 받으면 붙여넣고 끝나지만,
설명을 먼저 받으면 **AI가 틀렸을 때 눈치챌 수 있다.**

---

## Git 되돌리기

어제는 기록하는 법, 오늘은 되돌리는 법. **기록을 남기는 이유가 되돌아가기 위해서**라는 관점.

| 지금 상황 | 쓸 명령어 |
| --- | --- |
| 파일을 고쳤는데 되돌리고 싶다 (`add` 전) | `git restore <파일>` |
| `add` 했는데 빼고 싶다 | `git restore --staged <파일>` |
| 방금 커밋 메시지가 오타다 | `git commit --amend -m "..."` |
| 방금 커밋을 취소 | `git reset --soft HEAD~1` |
| 이미 `push` 한 커밋을 취소 | `git revert <해시>` |
| 날린 커밋을 되살리기 | `git reflog` → `git reset --hard <해시>` |

- `reset` 옵션은 `--soft`(저장소만) → `--mixed`(+스테이징) → `--hard`(+작업폴더 ⚠️) 순으로 더 지운다.
  헷갈리면 `--soft` 부터가 안전하다.
- **`reset` 은 장부를 찢는 것, `revert` 는 빨간 줄을 긋고 "취소"라고 적는 것.**
  공유된 기록에 `reset` 을 쓰면 남이 이미 받아간 히스토리와 어긋난다 → push 한 건 `revert`.
- **Fork** 는 clone과 달리 GitHub 서버 안에 내 저장소를 새로 만드는 것. 원본에 쓰기 권한이 없을 때.

---

## 실습 ① 자동완성으로 계좌 카드 만들기

> [`03_실습`](https://github.com/mgyokim/woori-wonit-ai-coding/tree/main/03_%EC%8B%A4%EC%8A%B5)

빈 `index.html` 에 주석 한 줄만 치고 자동완성을 받은 뒤, 전체 선택 → `Ctrl + I` 로 CSS를 붙였다.

> 마지막 미션이 **일부러 `Esc` 로 거절해보기**였다. "받아들이지 않는 것도 선택"이라는 걸 손에 남기는 게 목적.

---

## 실습 ② 낯선 코드 읽기

> [`01_코드읽기`](https://github.com/mgyokim/woori-wonit-ai-coding/tree/main/01_%EC%BD%94%EB%93%9C%EC%9D%BD%EA%B8%B0) — 동작은 정상이지만 함수명이 `f1`~`f4`, 변수명이 `a`·`tmp`·`arr2` 인 코드

실무에서 첫 달에 하는 일은 새 코드를 쓰는 게 아니라 **남이 쓴 코드를 읽는 일**이라는 전제.

- **"동작은 바꾸지 말고 이름만"** 을 안 적으면 AI가 로직까지 고쳐놔서,
  어디서부터 잘못됐는지 찾을 수 없게 된다.
- `f3` 설명을 받은 뒤 콘솔에서 `console.log(f3(TRANSACTIONS))` 로 **직접 실행해 확인**했다.
  AI 설명은 **문장이 매끄러울수록 더 믿음이 가는데, 그게 함정이다.**
- 주석은 코드를 옮긴 것(`i를 1 증가시킨다`)이 아니라 **"왜 이렇게 했는지"** 가 들어가야 한다.
- 파일을 통째로 다시 써서 오면 **"바뀌는 부분만 보여줘"** 를 붙인다.

---

## 실습 ③ AI로 디버깅하기 — 버그 5개

> [`02_버그잡기`](https://github.com/mgyokim/woori-wonit-ai-coding/tree/main/02_%EB%B2%84%EA%B7%B8%EC%9E%A1%EA%B8%B0) — 상세 분석은 해당 폴더 README

| # | 증상 | 원인 | 콘솔 에러 |
| :---: | --- | --- | :---: |
| 1 | 로드 즉시 알럿, 버튼은 무반응 | `addEventListener("click", handleDeposit())` — 괄호 때문에 즉시 실행되고 `undefined` 가 등록됨 | 없음 |
| 2 | 환율에 `[object Promise]` | `async` 함수 반환값을 `await` 없이 문자열과 결합 | 없음 |
| 3 | 잔액이 `-`, 입금해도 안 바뀜 | `getElementById("blance")` 오타 → `null` + 화면 재렌더 누락 | **있음** |
| 4 | 이자에 `26652.500000000004` | 원 단위 반올림 없음 + 부동소수점 오차 | 없음 |
| 5 | 이체 확인 모달이 안 보임 | `.modal-overlay` 에 `position` 누락 → `top`/`z-index` 무시 | 없음 |

**콘솔에 에러가 뜬 건 다섯 중 하나뿐이었다.** 나머지는 "에러는 없는데 결과가 이상한" 유형이라
에러 메시지가 아니라 **기대 동작과 실제 동작의 차이**에서 출발해야 찾을 수 있다.

원인 규명에서 갈렸던 두 개 —

- **4번은 원인이 세 겹**이었다. `.5` 는 계산이 맞아서 나온 값이고(이자는 원래 소수가 나온다),
  `000000000004` 는 2진 부동소수점 오차다. **서로 다른 두 문제가 한 줄에 같이 보이던 상황.**
  여기서 AI는 `toFixed(2)` 를 권하는데 **문자열이 되고 소수점도 남아서 틀렸다.**
  금액은 `Math.round()` 로 **원 단위 정수**로 다뤄야 한다.
- **5번은 문법 오류가 아니라 유효한 CSS**라서 콘솔에 아무것도 안 뜬다. `classList.remove("hidden")`
  도 실제로 실행돼서 Elements 탭엔 "열려 있는" 것으로 보인다. 개발자도구 **Computed 탭**에서
  `top` 에 취소선이 그어진 걸 보고서야 잡혔다.

> **CSS는 에러가 나지 않는다.** 브라우저는 이해 못 하는 CSS를 조용히 무시한다.
> 그래서 "뭐가 잘못됐다"가 아니라 **"지금 이렇게 보이는데 이렇게 보이고 싶다"** 로 물어야 한다.

수정 후 Node 내장 러너로 11개 케이스를 검증했다 (`node --test 02_버그잡기/app.test.js`).
다만 **5번은 가짜 DOM이 레이아웃을 계산하지 않아 테스트로 못 잡는다** — 브라우저에서 눈으로 봐야 한다.

---

## 회고

**Keep** — 버그를 잡을 때마다 원인을 한 문장으로 적고 넘어간 것. 5개 중 4개가 콘솔 에러가 없는
유형이라, 기록이 없었으면 다음에 같은 걸 또 헤맸다.

**Problem** — `f3` 설명을 AI에게 받았을 때 문장이 매끄러워서 그대로 믿을 뻔했다. 콘솔에서 직접
실행해보고서야 확인이 됐다. 검증이 아직 습관이 아니라 의식적으로 해야 하는 단계다.

**Try** — AI 답변을 붙여넣기 전에 "왜 이 코드인지" 한 문장을 커밋 메시지나 주석에 남긴다.
설명하지 못하면 그건 고친 게 아니라 붙여넣은 것이다.

> AI가 코드를 써준다면 사람이 하는 일은 **무엇을 물을지 정하고, 답이 맞는지 판단하는 일**이다.
> 이자 버그에서 AI는 문법적으로 멀쩡한 `toFixed(2)` 를 답으로 준다. 틀렸다고 알아보는 건 사람이다.
